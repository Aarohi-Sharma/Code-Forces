#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    vector<int> out(n);
    int a, b;
    for(int i=0; i<n; i++){
        cin>>a>>b;
        int c=b;
        out[i]=(c-a)+(b-c);
    }
    for(int i=0; i<n; i++){
        cout<<out[i]<<endl;
    }
}
